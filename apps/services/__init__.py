"""The services package is where different services apps live.

Each service app defines all the behaviour that is specific to a service in a
file named services.py.  It does this by extending an apps.service.Service
class, and then registering it (see the documentation in the apps.service app).
"""