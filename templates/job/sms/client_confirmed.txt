{% load core_tags humanize %}JOB CONFIRMED: Date: {% if job_request.date|istoday %}today{% else %}{{ job_request.date }}{% endif %} at {{ job_request.start_time }}.  For {{ job_request.duration }} hours at £{{ job_request.client_pay_per_hour.amount }}/hr.  Freelancer Name{{ job_request.bookings.count|pluralize }}: {% for booking in job_request.bookings.all %}{{ booking.freelancer }}{% if not forloop.last %}, {% endif %}{% endfor %}. Thanks for your booking!